import React from 'react';

const SectionHeading = ({ text }: { text: string }) => (
  <h2 className="text-4xl sm:text-5xl font-semibold text-center my-6 sm:my-8 ">{text}</h2>
);

export default SectionHeading;
